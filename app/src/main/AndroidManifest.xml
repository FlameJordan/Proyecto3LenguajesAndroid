<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ucr.cr.ac.proyecto3">



    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Proyecto3 final"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Proyecto3"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activities.DetallesVacunaActivity"></activity>
        <activity android:name=".Activities.VerVacunasActivity" />
        <activity android:name=".Activities.UpdatePacienteActivity" />
        <activity android:name=".Activities.DetallesAlergiaActivity" />
        <activity android:name=".Activities.VerAlergiasActivity" />
        <activity android:name=".Activities.DetallesCitaActivity" />
        <activity android:name=".Activities.VerCitasActivity" />
        <activity android:name=".Activities.SignUpActivity" />
        <activity
            android:name=".Activities.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>